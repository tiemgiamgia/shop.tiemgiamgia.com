{
  "name": "tiemgiamgia-shop",
  "type": "module",

  "scripts": {
    "dev": "astro dev",

    "generate": "node scripts/generate-products.js",

    "upload": "node scripts/upload-kv.js",

    "build": "astro build",

    "deploy": "npm run generate && npm run upload && wrangler deploy"
  },

  "dependencies": {
    "astro": "^4.0.0",
    "@astrojs/cloudflare": "^10.0.0"
  }
}